[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Show locally meaningful output
addopts =
    -v
    --tb=short
    --strict-markers

# Register custom markers
markers =
    slow: mark test as slow-running
    integration: mark as integration test (requires live DB)
